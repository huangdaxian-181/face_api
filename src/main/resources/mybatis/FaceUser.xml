<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.face.mapper.UserMapper">
    <select id="selectQuery" resultType="com.example.face.domain.FaceUser">
        select * from face_user where name = #{name}
    </select>
</mapper>